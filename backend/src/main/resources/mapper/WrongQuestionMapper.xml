<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.刷题.mapper.WrongQuestionMapper">
    
    <select id="selectByUserId" resultType="com.example.刷题.entity.WrongQuestion">
        SELECT id, user_id, question_id, user_answer, wrong_count, last_wrong_time, created_at
        FROM wrong_question
        WHERE user_id = #{userId}
        ORDER BY last_wrong_time DESC
    </select>
    
    <select id="selectByUserIdAndQuestionId" resultType="com.example.刷题.entity.WrongQuestion">
        SELECT id, user_id, question_id, user_answer, wrong_count, last_wrong_time, created_at
        FROM wrong_question
        WHERE user_id = #{userId} AND question_id = #{questionId}
    </select>
    
</mapper>
