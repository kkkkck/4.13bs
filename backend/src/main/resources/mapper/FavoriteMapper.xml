<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.刷题.mapper.FavoriteMapper">
    
    <select id="selectByUserIdAndQuestionId" resultType="com.example.刷题.entity.Favorite">
        SELECT id, user_id, question_id, created_at
        FROM favorite
        WHERE user_id = #{userId} AND question_id = #{questionId}
    </select>
    
    <delete id="deleteByUserIdAndQuestionId">
        DELETE FROM favorite
        WHERE user_id = #{userId} AND question_id = #{questionId}
    </delete>
    
</mapper>
